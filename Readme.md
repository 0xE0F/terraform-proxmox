# Terraform templates for Proxmox

## Secrets

The following environment variables are expected to be set:

```
export PM_API_TOKEN_ID=token_id
export PM_API_TOKEN_SECRET=token_secret_value

terraform apply
```
